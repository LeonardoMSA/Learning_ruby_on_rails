<h1>Todos os Artigos Disponíveis:</h1>

<table>
    <thead>
        <tr>
            <th>Título</th>
            <th>Descrição</th>
            <th>Ações</th>
        </tr>
    </thead>

    <tbody>
        <% @artigos.each do |artigo| %>
            <tr>
                <td><%= artigo.title %></td>
                <td><%= artigo.description %></td>
                <td>Placeholder</td>
            </tr>
        <% end %>
    </tbody>
</table>